# Auto-activate the project's virtualenv when using direnv
# This file is intended for use with direnv (https://direnv.net/).
# After adding this file you must run: direnv allow

# If a .venv exists, source its activate script. This is a simple and
# explicit activation that works on macOS/Linux. Direnv will run this
# automatically when you cd into the project (once you allow it).
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi

# Optional: expose the venv python through PATH explicitly (not required
# if activate was sourced above). Left commented for clarity.
# if [ -d .venv/bin ]; then
#   PATH_add .venv/bin
# fi
